<!--
Copilot / AI agent instructions for the magrizzly-web repository.
Generated: 2025-10-24
Purpose: Give immediate, actionable guidance so an AI coding agent can be productive editing this static web project.
-->

# magrizzly-web — AI agent instructions

Summary
- This repository is a minimal static website. The entrypoint is `index.html` at the repo root. Static assets live under `public/` with subfolders `assets/` and `styles/` (currently empty).

What you can safely change
- Update `index.html` to add content, sections, or links. Keep the document semantic and minimal. The project currently has no build step, server, or package manifest.
- Add files under `public/` for images, JS, or CSS. Prefer placing CSS in `public/styles/` and images in `public/assets/`.

Project conventions and patterns
- Single-page static site: No frameworks detected. Keep changes small and self-contained. Avoid introducing a heavy build system unless requested.
- File placement: `index.html` is the served root. Treat `public/` as the webroot for static files.
- Minimalist HTML: Follow existing simple structure — head meta tags, a title, and body content. If you add scripts or styles, reference them with relative paths (e.g., `public/styles/site.css` -> use `./public/styles/site.css` in `index.html`).

Developer workflows (discoverable)
- There is no package.json, build scripts, or tests present. Standard local testing is done by opening `index.html` in a browser or serving the folder with a static server.
  - Quick local preview suggestions (for humans): `npx serve .` or `npx http-server .` from the repo root. Don't add these as project dependencies without owner's consent.
  - Explicit PowerShell example to preview locally (human):

    ```powershell
    # install a tiny static server if you don't have one
    npx serve .
    # or
    npx http-server .
    ```
    (Run these from the repo root in PowerShell.)

Integration points & external dependencies
- None detected. No back-end, APIs, or third-party SDKs are present in the repository files.

Guidance for code changes
- Be explicit in PRs: mention file paths changed and why (e.g., "added hero section in `index.html` and `public/styles/hero.css`").
- Keep changes backward-compatible: the site is static and likely served as-is; avoid renaming `index.html` or moving the file unless you update the consumer or deployment configuration.
- When introducing JS/CSS, prefer small, dependency-free snippets. If a third-party library is required, propose adding a package manager and a clear migration plan.

Examples from this repo
- Entrypoint: `index.html` — modify the <title>, meta, and body content here.
- Static folders: `public/styles/` (CSS) and `public/assets/` (images) — create these files when adding visual or interactive changes.

- Starter assets: `public/styles/site.css` — a minimal stylesheet was added; use this as an example of placement and simple responsive patterns. Link from `index.html` with `./public/styles/site.css`.

PR checklist (useful for AI-generated PRs)
- Short summary of changes and motivation (files changed).
- Confirm `index.html` remains at repo root and newly-added links use `./public/...`.
- Avoid adding package managers (package.json) or build tooling without owner's explicit approval.
- If adding JS or CSS, keep it dependency-free and small. For larger changes, include brief docs in the PR.

If you are unsure
- Ask the repository owner before adding build tooling or package manifests.

Notes
- This instruction file was generated by analyzing the repository as of 2025-10-24. If you add server code, build configs, or package manifests later, update this file to reflect the new workflows.
